.container{style: "position:relative;"}
  - @campaigns.each do |campaign|
    .col-lg-3.col-md-3.col-sm-4.col-xs-6
      .profile-well
        %h4= campaign.title
        %p= campaign.details.truncate(50)
        %p= link_to "View", "javascript:void(0);"
        %p= link_to "View", campaign, class: "btn btn-primary", remote: true, data: {toggle: "modal", target: "#campaign-view"}


= will_paginate @campaigns, renderer: BootstrapPagination::Rails

/NOTE for using Faker to populate data, following can be used in Rails C:
/ 100.times{ Campaign.create(title: Faker::Company.bs, details: Faker::Lorem.paragraph, target: rand(1000000), end_date:(Time.now+10.day) )}
